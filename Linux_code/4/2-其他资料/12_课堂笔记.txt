1. 信号的概念
    基本的属性
    信号的要素
2. 产生信号5种
    1) kill，raise,abort
    2) alarm函数，定时器
    3) setitimer函数
    4) 硬件产生：非法访问，除0，内存对齐出错
    5) 按键 ctrl + c
3. 信号集操作函数
    信号屏蔽字
    未决信号集
4. 信号的捕捉
    注册信号捕捉函数
    sigaction函数
5. 未决信号集，信号屏蔽集
    1）递达并且到达进程
    2）产生和递达之间的状态，主要由于阻塞导致该状态
6. 处理方式：
    1）执行默认动作
    2）忽略（丢弃）
    3）捕捉（调用户处理函数）
7. 信号四要素：
    1）名字
    2）编号
    3）默认处理动作
    4）事件
8. 常用的信号
    2）SIGINT

9. 默认动作
    1）Term,终止进程，kill
    2）lgn,信号忽略
    3）core,生成core文件，用于内核调试
    4）stop,进程暂停
    5）Cont,进程继续，忽略信号。
10. 不可以忽略的信号和捕捉，只能执行默认动作
    1）SIGKILL
    2）SIGSTOP
11. 终端按键产生
    1）Ctrl + c，SIGINT，进程中断
    2）Ctrl + z，SIGTSTP，
12. 