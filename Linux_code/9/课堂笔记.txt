错误处理：
    read返回值：
        1. > 0 实际读到的字节数buf = 1024，一般情况下buf=1024，当出现小于1024时，数据读到末尾
        2. = 0 数据读完，
        3. -1 异常
            1. errno == EINTR 被信号中断，重启或者quit
            2. errno == EAGIAN（EWOULDBLOCK），非阻塞方式读，并且没有数据。
            3. 其他值，出现错误，quit。
    write:
        1. > 0 实际写入的值